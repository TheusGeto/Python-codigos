import os
import time
os.system("cls || clear")
#Desenvolva um programa que, com um laço `for`, exiba a sequência de números começando de 10 e decrementando até 1.

for i in range(10,0,-1):
     print(f" Numero:  {i}º")
     time.sleep(0.4)
